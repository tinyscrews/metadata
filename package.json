{
  "name": "@tinyscrews/metadata",
  "version": "0.2.0",
  "description": "Utilities for TypeScript decorators, define and inspect metadata",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "prepublishOnly": "npm i --package-lock-only && npm ci && npm run clean && npm run build && npm run test",
    "clean": "npx rimraf lib *.tsbuildinfo",
    "build": "tsc",
    "test": "echo \"Error: no test specified\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "repository": "github:tinyscrews/metadata",
  "bugs": {
    "url": "https://github.com/tinyscrews/metadata/issues"
  },
  "dependencies": {
    "@abraham/reflection": "^0.9.1"
  },
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}
